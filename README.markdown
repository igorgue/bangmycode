# INSTALL

## Install node:

`brew install node`

## Add node path:

`echo 'export NODE_PATH="/usr/local/lib/node"' >> ~/.profile`

`source ~/.profile`

## Install npm:

`curl http://npmjs.org/install.sh | sh`

## Install express:

`npm install express`

